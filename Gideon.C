//a program to accept two integers from the user and calculate the product of the two integers
#include<stdio.h>
int main ()
{
    int j,k,p;
    print("\n enter the first number:");
    scanf("%d",&j);
    printf("\n enter the second number:");
    scanf("%d",&k);
    p=j*k;
    printf("the product is %d\n",p);
}