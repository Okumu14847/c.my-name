// a program that accepts two integers from the user and calculate the product of the two integers
#include <stdio.h>
int main ()
{
    int j,k,p;
    printf("\n enter the first number:");
    scanf("%d",&j);
    printf("\n enter the second number:");
    scanf("%d",&k);
    p=j*k;
    printf("the product is %d\,p);}